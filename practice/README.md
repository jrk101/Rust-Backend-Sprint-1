# Individual Practice Zone

Create one folder per cohort member after student IDs are finalized:

```text
practice/
├── RB01/
│   ├── unit-01/
│   └── ...
└── RBxx/
```

Do not generate empty copies until cohort size and IDs are known. Unit files
describe the required paths relative to each student's unit folder.

